git --version
git config --list
git config --global user.name "Name"
git config --global user.email "Email"
git status
git init
git add .
git commit -m "message or not"
git log
git remote add origin (path)
git push -u origin main
git clone (repo url)